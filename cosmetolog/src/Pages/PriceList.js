import React from 'react';
import ReactDOM from 'react-dom';

export default class PriceList extends React.Component {

  constructor(props){
    super(props);
  }

  render() {
    return(
      <h1>This is PriceList!</h1>
    );
  }
}
